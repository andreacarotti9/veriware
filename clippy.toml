# Public API takes adversarial input; unwrap/expect in library code is a
# reachable panic. Enforced by `#![deny(clippy::unwrap_used, clippy::expect_used)]`
# in core/src/lib.rs, which this threshold complements for arithmetic.
allow-panic-in-tests = true
